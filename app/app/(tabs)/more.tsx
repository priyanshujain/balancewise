import { View } from 'react-native';

export default function MoreScreen() {
  // This screen is just a placeholder - the actual more options will be shown in a modal
  return <View className="flex-1 bg-transparent" />;
}
